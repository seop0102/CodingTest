#include <iostream>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    // 시간 복잡도를 구하는 문제
    int n;
    cin >> n;
    cout << 1 << "\n" << 0;
    return 0;
}